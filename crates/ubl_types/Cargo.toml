[package]
name = "ubl_types"
version = "0.1.0"
edition = "2021"
description = "Leaf-level newtypes for UBL: Cid, Did, Kid, Nonce. Zero heavy deps."

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
